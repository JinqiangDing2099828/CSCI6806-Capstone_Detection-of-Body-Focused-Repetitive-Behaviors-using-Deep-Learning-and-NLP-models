# Data

This folder contains the raw dataset and all processed data used in our experiments.

- `raw/` contains the original data.
- `processed/` includes cleaned and normalized data.
- `processed_data_tof/` contains processed files used for TOF-related experiments and model evaluation.
